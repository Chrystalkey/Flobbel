CREATE TABLE IF NOT EXISTS files(id BIGINT PRIMARY KEY AUTOINCREMENT NOT NULL, hash VARCHAR(32), filesize BIGINT NOT NULL, compHash VARCHAR(32), compFileSize BIGINT NOT NULL, computerHandle TEXT, saveTimestamp TEXT);
CREATE TABLE IF NOT EXISTS servers(id BIGINT PRIMARY KEY AUTOINCREMENT NOT NULL, ip INT, port INT, login TEXT, pass TEXT); --encrypt DATABASE